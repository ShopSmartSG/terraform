#name: Terraform Destroy
#
#on:
#  workflow_dispatch:
#    inputs:
#      action:
#        description: 'Choose action to perform'
#        required: true
#        default: 'apply'
#        options:
#          - plan-only
#          - plan-destroy
#
#jobs:
#  terraform-plan-destroy:
#    name: Plan Infrastructure Destruction
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      - name: Set up Terraform
#        uses: hashicorp/setup-terraform@v2
#        with:
#          terraform_version: 1.5.0
#
#      - name: Configure AWS Credentials
#        uses: aws-actions/configure-aws-credentials@v1
#        with:
#          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          aws-region: ${{ secrets.AWS_REGION }}
#
#      - name: Terraform Init
#        run: terraform init
#
#      - name: Terraform Plan Destroy
#        id: plan
#        run: terraform plan -destroy -out=destroy.out
#
#      - name: Upload Destroy Plan
#        uses: actions/upload-artifact@v3
#        with:
#          name: terraform-destroy-plan
#          path: destroy.out
#
#  terraform-apply-destroy:
#    name: Apply Infrastructure Destruction
#    runs-on: ubuntu-latest
#    needs: terraform-plan-destroy
#    if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'plan-destroy'
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      - name: Set up Terraform
#        uses: hashicorp/setup-terraform@v2
#        with:
#          terraform_version: 1.5.0
#
#      - name: Configure AWS Credentials
#        uses: aws-actions/configure-aws-credentials@v1
#        with:
#          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          aws-region: ${{ secrets.AWS_REGION }}
#
#      - name: Terraform Init
#        run: terraform init
#
#      - name: Terraform Plan (Re-plan)
#        run: terraform plan -out=destroy.out
#
#      - name: Terraform Apply Destroy
#        run: terraform apply -auto-approve destroy.out
